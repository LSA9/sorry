/**
 * Created by Aronson1 on 11/1/15.
 */

var currentId
var currentx
var currenty
var start

function getActiveId(e){
    currentId = e.id
    currentx = e.offsetLeft
    currenty = e.offsetTop
    inStart()
}

function inStart(){
    if(currentx)
}